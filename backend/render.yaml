services:
  - type: web
    name: inox-pharma-api
    env: node
    rootDir: backend
    buildCommand: npm ci && node node_modules/.bin/prisma generate && node node_modules/typescript/bin/tsc
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production